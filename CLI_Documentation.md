# CLI Documentation - Система автоматического формирования кредитных скоринговых моделей

## Обзор

CLI предоставляет интерфейс командной строки для работы с системой автоматического формирования кредитных скоринговых моделей на основе генетических алгоритмов.

## Структура команд

CLI содержит 4 основные команды:

1. **`run-chromosome`** - Запуск конкретной хромосомы с обучением модели
2. **`run-ga`** - Запуск генетического алгоритма для поиска оптимального пайплайна
3. **`predict`** - Применение обученной модели к новым данным
4. **`list-models`** - Просмотр всех доступных моделей

---

## Команды

### 1. `run-chromosome`

**Описание**: Запускает конкретную хромосому (набор из 20 генов, кодирующих ML-пайплайн) с полным обучением модели и сериализацией.

**Синтаксис**:
```bash
python cli.py run-chromosome --chromosome CHROMOSOME --dataset DATASET --target TARGET [--learning-curves]
```

**Обязательные параметры**:
- `--chromosome` - Хромосома в формате "1,2,0,1,..." (точно 20 генов через запятую)
- `--dataset` - Путь к CSV файлу с датасетом
- `--target` - Название целевой колонки в датасете

**Опциональные параметры**:
- `--learning-curves` - Генерировать кривые обучения (флаг)

**Кодировка хромосомы** (20 генов):
- Гены 0-3: Импутация пропущенных значений
- Гены 4-7: Обработка выбросов  
- Гены 8-11: Балансировка классов
- Гены 12-15: Кодирование категориальных признаков
- Гены 16-19: Масштабирование признаков
- Ген 20: Модель (0=LogisticRegression, 1=RandomForest, 2=SVM, 3=NN)

**Примеры**:
```bash
# Запуск логистической регрессии с базовой предобработкой
python cli.py run-chromosome \
  --chromosome "1,2,0,1,1,2,0,1,0,0,1,1,1,0,0,1,2,3,1,0" \
  --dataset "datasets/diabetes.csv" \
  --target "Outcome"

# С генерацией кривых обучения
python cli.py run-chromosome \
  --chromosome "0,1,0,0,2,1,0,0,1,0,0,1,0,1,0,0,1,2,0,1" \
  --dataset "datasets/credit.csv" \
  --target "default" \
  --learning-curves
```

**Выходные данные**:
- Метрики качества модели (AUPRC, точность, полнота и др.)
- Сериализованная модель в папке `models/`
- Метаданные модели в JSON формате
- Кривые обучения (если указан флаг)

---

### 2. `run-ga`

**Описание**: Запускает генетический алгоритм для поиска оптимального ML-пайплайна.

**Синтаксис**:
```bash
python cli.py run-ga --train DATASET --target TARGET [OPTIONS]
```

**Обязательные параметры**:
- `--train`, `--dataset` - Путь к CSV файлу с обучающим датасетом
- `--target` - Название целевой колонки

**Опциональные параметры**:
- `--population INT` - Размер популяции (по умолчанию: 10)
- `--generations INT` - Количество поколений (по умолчанию: 8)
- `--elitism FLOAT` - Процент элитизма 0.0-1.0 (по умолчанию: 0.25)
- `--mutation FLOAT` - Вероятность мутации 0.0-1.0 (по умолчанию: 0.1)
- `--tournament INT` - Размер турнира для селекции (по умолчанию: 3)
- `--learning-curves` - Генерировать кривые обучения для лучшей модели (флаг)
- `--auto-save`, `--save-best` - Автоматически сохранить лучшую модель (флаг)

**Примеры**:
```bash
# Базовый запуск ГА
python cli.py run-ga \
  --train "datasets/diabetes.csv" \
  --target "Outcome"

# Расширенная настройка с сохранением лучшей модели
python cli.py run-ga \
  --dataset "datasets/credit.csv" \
  --target "default" \
  --population 20 \
  --generations 15 \
  --elitism 0.3 \
  --mutation 0.15 \
  --tournament 5 \
  --auto-save \
  --learning-curves

# Быстрый тест с малой популяцией
python cli.py run-ga \
  --train "datasets/small_dataset.csv" \
  --target "target" \
  --population 5 \
  --generations 3
```

**Выходные данные**:
- Прогресс эволюции по поколениям
- Статистика лучших особей каждого поколения  
- Финальные результаты с лучшей хромосомой
- Сериализованная лучшая модель (при флаге `--auto-save`)
- Подробные результаты исследования в папке `research/`

---

### 3. `predict`

**Описание**: Применяет обученную модель к новым данным для получения предсказаний.

**Синтаксис**:
```bash
python cli.py predict --model MODEL --data DATA [--output OUTPUT]
```

**Обязательные параметры**:
- `--model` - Имя модели (без расширения) или полный путь к модели
- `--data` - Путь к CSV файлу с данными для предсказания

**Опциональные параметры**:
- `--output` - Путь для сохранения результатов (по умолчанию: `results/predictions_TIMESTAMP.csv`)

**Примеры**:
```bash
# Базовое предсказание с автоматическим именем файла
python cli.py predict \
  --model "diabetes_logistic_regression_20250608_120000" \
  --data "new_patients.csv"

# С указанием выходного файла
python cli.py predict \
  --model "credit_random_forest_ga_best" \
  --data "loan_applications.csv" \
  --output "results/loan_predictions.csv"

# Использование полного пути к модели
python cli.py predict \
  --model "../models/custom_model" \
  --data "test_data.csv"
```

**Формат выходных данных**:
CSV файл содержит:
- Все исходные колонки из входного файла
- `prediction` - предсказанный класс (0 или 1)
- `probability_class_0` - вероятность класса 0
- `probability_class_1` - вероятность класса 1

**Требования к входным данным**:
- CSV файл с теми же признаками, что использовались при обучении
- Отсутствие целевой колонки (она будет предсказана)
- Совместимая схема данных

---

### 4. `list-models`

**Описание**: Показывает список всех доступных моделей с их характеристиками.

**Синтаксис**:
```bash
python cli.py list-models [--verbose]
```

**Опциональные параметры**:
- `--verbose` - Показать подробную информацию, включая хромосомы (флаг)

**Примеры**:
```bash
# Базовый список моделей
python cli.py list-models

# Подробная информация
python cli.py list-models --verbose
```

**Выходная информация для каждой модели**:
- 🤖 **Имя модели**
- 📊 **Датасет** - исходный датасет
- 🎯 **Целевая переменная**
- 📈 **Метрика** - AUPRC или точность
- 📅 **Дата создания**
- 🔧 **Источник** - manual (run-chromosome) или ga (run-ga)
- 💾 **Размер модели** и тип хранения
- 🧬 **Хромосома** (только с флагом --verbose)

**Типы моделей**:
- **Полная модель** - содержит сериализованную модель + метаданные
- **Legacy модель** - старый формат .pkl файла
- **Метаданные только** - только JSON с информацией о модели

---

## Структура файлов

### Входные файлы
- **Датасеты**: CSV файлы в папке `datasets/`
- **Новые данные**: CSV файлы для предсказаний

### Выходные файлы
- **Модели**: Папка `models/` - сериализованные модели и метаданные
- **Предсказания**: Папка `results/` - результаты предсказаний
- **Исследования**: Папка `research/` - результаты ГА и эксперименты

### Формат модели
```
models/
├── model_name/
│   ├── model.joblib          # Основная модель
│   ├── scaler.joblib         # Скейлер (если есть)
│   ├── encoder.joblib        # Энкодер (если есть)
│   └── keras_model/          # TensorFlow модель (если NN)
└── model_name_metadata.json  # Метаданные модели
```

---

## Примеры полного цикла работы

### Сценарий 1: Ручная настройка модели
```bash
# 1. Обучение конкретной модели
python cli.py run-chromosome \
  --chromosome "1,0,1,0,2,1,0,0,1,0,0,1,0,1,0,0,1,2,0,1" \
  --dataset "datasets/credit.csv" \
  --target "default" \
  --learning-curves

# 2. Просмотр созданной модели
python cli.py list-models --verbose

# 3. Применение к новым данным
python cli.py predict \
  --model "credit_random_forest_20250608_143022" \
  --data "new_applications.csv" \
  --output "results/credit_predictions.csv"
```

### Сценарий 2: Автоматическая оптимизация
```bash
# 1. Запуск генетического алгоритма
python cli.py run-ga \
  --dataset "datasets/diabetes.csv" \
  --target "Outcome" \
  --population 15 \
  --generations 10 \
  --auto-save

# 2. Проверка результатов
python cli.py list-models

# 3. Использование лучшей модели
python cli.py predict \
  --model "diabetes_ga_best_20250608_150000" \
  --data "new_patients.csv"
```

---

## Коды возврата

- **0** - Успешное выполнение
- **1** - Ошибка выполнения или неверные параметры

---

## Технические детали

### Поддерживаемые модели
- **Logistic Regression** - Логистическая регрессия
- **Random Forest** - Случайный лес  
- **SVM** - Метод опорных векторов
- **Neural Network** - Нейронная сеть (TensorFlow/Keras)

### Методы предобработки
- **Импутация**: среднее, медиана, мода, удаление
- **Выбросы**: IQR, Z-score, изоляционный лес, без обработки
- **Балансировка**: SMOTE, undersampling, oversampling, без балансировки
- **Кодирование**: one-hot, label, target, без кодирования
- **Масштабирование**: StandardScaler, MinMaxScaler, RobustScaler, без масштабирования

### Метрики качества
- **AUPRC** - Area Under Precision-Recall Curve (основная метрика)
- **Accuracy** - Точность
- **Precision** - Точность положительного класса
- **Recall** - Полнота
- **F1-Score** - F1 мера
- **ROC AUC** - Area Under ROC Curve

---

## Устранение неполадок

### Частые ошибки

1. **Модель не найдена**
   ```
   ❌ Модель 'model_name' не найдена
   ```
   - Проверьте имя модели командой `python cli.py list-models`
   - Убедитесь, что модель была успешно сохранена

2. **Неверный формат хромосомы**
   ```
   ❌ Хромосома должна содержать точно 20 генов
   ```
   - Хромосома должна содержать ровно 20 чисел через запятую
   - Пример: "1,2,0,1,1,2,0,1,0,0,1,1,1,0,0,1,2,3,1,0"

3. **Несовместимые данные**
   ```
   ❌ Несовместимость схемы данных
   ```
   - Убедитесь, что входные данные имеют те же колонки, что и обучающие
   - Проверьте типы данных и отсутствие целевой колонки

4. **Недостаточно данных**
   ```
   ❌ Недостаточно данных для обучения
   ```
   - Проверьте размер датасета (минимум 50 записей)
   - Убедитесь в наличии разных классов в целевой переменной

### Логи и отладка

Все операции логируются в консоль с цветными эмодзи-индикаторами:
- 🚀 - Начало операции
- ✅ - Успешное завершение  
- ❌ - Ошибка
- 📊 - Метрики и результаты
- 💾 - Сохранение файлов
- 🤖 - Информация о моделях

Для детальной отладки используйте подробный вывод с флагом `--verbose` там, где он доступен. 